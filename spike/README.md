# Spike

This directory contains small, single purpose implementations of basic
functionality used in the unid client/server. The implementations are
not error free nor are they production quality. These are a springboard
to implementations in the client/server application.
