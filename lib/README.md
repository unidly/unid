# Lib packages used by Unid

Most third party libraries are managed with Conan. Packages in this directory
use Bazel as the build tool, and are not easily integrated using Conan.

TODO - develope a conan recipy to integrate these packages to support a
single "package" manager.
