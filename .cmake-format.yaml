format:
  line_width: 80
  tab_size: 2
  # Forces vertical layout if arguments exceed this number (set to 1 for vertical)
  max_pargs_hwrap: 1
  max_subgroups_hwrap: 2
  dangle_parens: true
  dangle_align: prefix

